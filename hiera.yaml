---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "Module Specific"
    path: "minebox.yaml"
lookup_options:
  minebox:
    merge:
      strategy: deep
      merge_hash_arrays: true